# ──────────────────────────────────────────────────────────────────────────────
# Devcontainer – Passos Mágicos Datathon
# Nota: este Dockerfile não é mais usado — o devcontainer.json aponta para
# a imagem diretamente via "image". Mantido como referência.
# ──────────────────────────────────────────────────────────────────────────────
FROM mcr.microsoft.com/devcontainers/python:3.10-bullseye
# TensorFlow instala HDF5 embutido via pip — sem apt customizado necessário
EXPOSE 8501 8888
USER vscode

